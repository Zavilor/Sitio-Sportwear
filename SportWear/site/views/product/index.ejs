<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/productList.css">
    <title>Products | SportWear</title>

    <%- include('../partials/header') %>
    
    <section class="sectionProductList"> 

        <div class="productDetailMainContainer">
            <div>
                <select name="showProds" id="">
                    <option value="5">5</option>
                    <option value="10">10</option>
                    <option value="15">15</option>
                    <option value="?limit=5">20</option>
                </select>
                <span>Ordenar:</span>
                <a href="?LIMIT=5">Titulo</a>
                <a href="?order=price">Rating</a>
            </div>
        

        <% products.forEach(products =>{ %>

            <div class="productDetailContainer">

                <div class="productImagen">
                    <a href="/product/<%= products.id %>">  
                        <img src= "<%= products.image %>" class="productImages" alt="...">
                </div>
                
                <div class="productList">
                    
                    <div class="prodNameContainer">                       
                        <p class="prodName"><%= products.name %></p>                      
                    </div>
                    <div class="precioProducto">
                        <p class="prodPrice">Precio : $<%= products.price %></p>
                    </a>
                    </div> 
                    <div class= "buttonsProductList">
                        <form action="cart/index" method="POST">
                            <button type="submit" class="btnProdList btn btn-success">Añadir al Carrito</button>
                        </form>
                        <!--<button type="submit" class="btnProdList btn btn-success" >Comprar Ahora</button> -->
                        <% if (locals.logeado) {%>
                        <button type="submit" class="btnProdList btn btn-light"><a href="/product/<%= products.id %>/edit">Editar</a></button>
                        <% } %>                                              
                    </div>

                </div>
                
            </div> 

            <% }) %>

        
         </div>  
            
        </section>

        
    <%- include('../partials/footer'); %>